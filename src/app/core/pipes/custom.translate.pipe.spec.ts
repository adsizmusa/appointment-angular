import { Custom.TranslatePipe } from './custom.translate.pipe';

describe('Custom.TranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new Custom.TranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
